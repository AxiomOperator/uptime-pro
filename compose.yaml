services:
  uptime-pro:
    build:
      context: .
      dockerfile: docker/dockerfile
      target: release
    image: uptime-pro:local
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      # <Host Port>:<Container Port>
      - "3001:3001"
